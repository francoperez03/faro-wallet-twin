---
phase: 04-solvencia-visible
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - circuits-mini/liabilities_batch_mini/Nargo.toml
  - circuits-mini/liabilities_batch_mini/src/main.nr
  - circuits-mini/liabilities_batch_mini.json
  - deployments.json
  - lib/config/tokens.ts
  - lib/sobrecito-mini/prove.ts
  - app/api/cuenta/corte-mini/route.ts
  - vercel.json
  - lib/db/schema.sql
autonomous: false
requirements: [SOL-06]

must_haves:
  truths:
    - "El smoke test de bb.js corre antes que cualquier otra tarea de este plan; si falla, el resto NO arranca y el plan cierra documentando el abandono"
    - "Si el smoke pasa: el circuito mini publica al segundo Registry, el pipeline corre por cron y por botón manual, y la app lo lee via REGISTRIES"
  artifacts:
    - path: "circuits-mini/liabilities_batch_mini/Nargo.toml"
      provides: "Crate Noir propio del repo twin (no del workspace de Sobrecito), path dependency read-only a commitment_lib"
    - path: "circuits-mini/liabilities_batch_mini.json"
      provides: "Artifact ACIR compilado una vez en la Mac (D-02), commiteado, consumido por bb.js en runtime"
    - path: "lib/sobrecito-mini/prove.ts"
      provides: "Proving con bb.js (WASM) dentro de una función de Vercel (Fluid, 300s), sin binarios nativos"
    - path: "app/api/cuenta/corte-mini/route.ts"
      provides: "Endpoint 'correr corte' manual + handler reusado por el cron"
    - path: "lib/db/schema.sql"
      provides: "CREATE TABLE IF NOT EXISTS openings (extiende el schema de Phase 3 con la tabla de D-09, idempotente)"
  key_links:
    - from: "app/api/cuenta/corte-mini/route.ts"
      to: "lib/sobrecito-mini/prove.ts"
      via: "export ledger -> commit_totals -> proof bb.js -> CutInput"
      pattern: "prove|CutInput"
    - from: "app/api/cuenta/corte-mini/route.ts"
      to: "segundo Registry"
      via: "walletClient (PUBLISHER_PRIVATE_KEY) firma publish()"
      pattern: "walletClient|publish"
---

<objective>
Stretch NO bloqueante de SOL-06: reemplazar la fixture por un "corte mini" real, generado sobre el ledger de la demo (≤64 usuarios, un solo lote), probado con Poseidon2/UltraHonk vía bb.js en runtime y publicado a un segundo Registry.

Purpose: es la única pieza del roadmap que toca Noir/bb.js fuera de Sobrecito. El gate de D-05 (Task 1) decide si el resto de este plan corre. Si el gate falla, el plan se abandona ahí y la fase igual cierra completa: SOL-03/04/05 (Plans 01-02) no dependen de esto, y Phase 5 tampoco.
Output, si el gate pasa: artifact del circuito mini commiteado, segundo Registry deployado, pipeline de proving+publish corriendo por cron diario y por botón manual, `REGISTRIES` (Plan 01) con una segunda entrada "Corte real (mini)".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@SPEC.md#7.3
@SPEC.md#7.7
@.planning/phases/04-solvencia-visible/04-CONTEXT.md
@.planning/phases/02-sobrecito-registry/02-CONTEXT.md
@.planning/phases/04-solvencia-visible/04-01-PLAN.md
@.planning/phases/04-solvencia-visible/04-02-PLAN.md
</context>

<interfaces>
<!-- Public inputs layout del target evm, igual al de la fixture (manifest.json de Sobrecito):
     [key_hash, C_L]. El mini reusa exactamente ese layout (D-01), así que el SobrecitoRegistry.sol
     existente (sin cambios) sirve también para el segundo deploy, anclado a un keyHash distinto. -->

```
liabilities_batch (variante mini, D-01):
  K=64, T=2 (ARGt + BOLt si aplica; T=1 si solo ARGt en esta demo — confirmar con el schema real de Phase 3)
  output público: C_L = commitment_lib::commit_totals(totals, acc_root, r)
  Un solo lote: sin árbol de agregación recursiva (el ledger de la demo entra en K=64)

CutInput (SobrecitoRegistry.sol, sin cambios):
  { corteId, cL, cR, blockB, verdicts[], coverageBps[], attestationHash }
  cL = C_L probado on-chain (publicInputs[1])
  cR/verdicts/coverageBps/attestationHash = declarados, mismo criterio que Phase 2
```

`gen-verifier.sh` y `Deploy.s.sol` viven en `/Users/francoperez/repos/job/Sobre/sobrecito/contracts/`. Mismo patrón de deploy que Phase 2 (D-05 de 02-CONTEXT.md), publisher = wallet M2, clave por command substitution, jamás en archivos.
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Smoke test de bb.js (D-05) — gate del stretch</name>
  <files>circuits-mini/liabilities_batch_mini/Nargo.toml, circuits-mini/liabilities_batch_mini/src/main.nr, circuits-mini/liabilities_batch_mini.json</files>
  <read_first>
    04-CONTEXT.md D-01, D-02, D-03, D-05. Sobrecito `circuits/README.md` (toolchain pineado, targets de verifier). Sobrecito `circuits/scripts/gen-verifier.sh` si existe (confirmar ruta exacta antes de invocarlo). 04-02-PLAN.md Task 1 (mismo patrón de crate propio con path dependency, aplicado ahí para `tools/poseidon2-vector/`).
  </read_first>
  <action>
    Con el toolchain pineado en la Mac (`nargo 1.0.0-beta.22`, `bb 5.0.0-nightly.20260522`, D-02), crear el crate `liabilities_batch_mini` DENTRO DEL REPO TWIN, en `circuits-mini/liabilities_batch_mini/`, con su propio `Nargo.toml` (proyecto standalone, no un miembro del workspace de Sobrecito) declarando una dependencia de path READ-ONLY a `commitment_lib` (`/Users/francoperez/repos/job/Sobre/sobrecito/circuits/commitment_lib`), mismo patrón que 04-02-PLAN.md Task 1 usa para `tools/poseidon2-vector/`. NUNCA editar `circuits/Nargo.toml` (el manifest del workspace) ni ningún otro archivo dentro del repo Sobrecito: agregar un miembro a esa lista es una edición real del repo, prohibida. Implementar en `src/main.nr` la variante mini de D-01 (K=64, T=1 o T=2 según el schema real de Phase 3, output `C_L` vía `commitment_lib::commit_totals`, public inputs `[key_hash, C_L]`). Compilar con `nargo compile` (target evm) desde `circuits-mini/liabilities_batch_mini/` y copiar el artifact ACIR resultante a `circuits-mini/liabilities_batch_mini.json` en la raíz del repo twin (D-02: "el artifact JSON del circuito se commitea en este repo"). Generar el verifier Solidity con el script de generación de verifiers del repo Sobrecito (nombre exacto a confirmar en `circuits/scripts/`), invocado apuntando al artifact standalone (no requiere que el crate sea miembro del workspace). Smoke test: con datos de prueba (K=64 balances dummy), generar una proof usando `bb` nativo (para tener un baseline) Y usando `bb.js` (paquete npm, versión compatible con `bb 5.0.0-nightly.20260522`) para el MISMO circuito y mismos inputs. Verificar la proof de bb.js contra el verifier Solidity generado, vía un `forge test` local (sin broadcast, sin fondos) que llama `verifier.verify(proof_bbjs, publicInputs)`. El smoke PASA si esa call devuelve `true`.
  </action>
  <how-to-verify>
    1. Confirmar `nargo --version` = `1.0.0-beta.22` y `bb --version` = `5.0.0-nightly.20260522`.
    2. Correr la compilación del crate mini y la generación del verifier.
    3. Correr el `forge test` que verifica la proof de bb.js contra el verifier Solidity.
    4. Reportar el resultado exacto (pass/fail) y, si falla, el error completo.
  </how-to-verify>
  <resume-signal>Escribí "smoke ok" si el forge test pasó (Task 2 y 3 arrancan), o pegá el error y "smoke falló" (el plan se abandona acá, SOL-06 queda sin cumplir y se documenta en el summary sin bloquear la fase)</resume-signal>
  <acceptance_criteria>
    Si "smoke ok": `circuits-mini/liabilities_batch_mini/Nargo.toml` existe con path dependency a `commitment_lib`, y `circuits-mini/liabilities_batch_mini.json` es el artifact real usado en el smoke (no un archivo vacío). El repo Sobrecito no tiene ningún archivo modificado (`git status` limpio ahí). Si "smoke falló": ningún otro archivo de este plan se crea, el summary documenta el abandono y por qué.
  </acceptance_criteria>
  <verify>
    <automated>test -f circuits-mini/liabilities_batch_mini.json && node -e "const a=require('./circuits-mini/liabilities_batch_mini.json'); if(!a || Object.keys(a).length===0) process.exit(1)"</automated>
  </verify>
  <done>Gate resuelto: se sabe con certeza si bb.js verifica contra el verifier generado con bb nativo, antes de invertir en el resto del pipeline.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy del segundo Registry anclado al VK mini (solo si Task 1 = "smoke ok")</name>
  <files>deployments.json, lib/config/tokens.ts</files>
  <read_first>
    02-CONTEXT.md completo (D-01 a D-11: mismo runbook de deploy, mismo publisher, mismo preflight). 04-CONTEXT.md D-04. 04-01-PLAN.md (forma exacta de `REGISTRIES` en `lib/config/tokens.ts`, para agregar la entrada mini sin romper la de la fixture).
  </read_first>
  <action>
    Si Task 1 no llegó a "smoke ok", saltar este task por completo (no crear ningún archivo) y dejarlo documentado como no ejecutado en el summary. Si "smoke ok": repetir el runbook de deploy de Phase 2 (preflight de balance ETH del publisher, `forge script Deploy.s.sol` apuntando al key_hash del circuito mini en vez del de la fixture, publisher = misma wallet M2) para deployar un SEGUNDO `SobrecitoRegistry` + el `HonkVerifier` generado en Task 1, en Arbitrum. Registrar el deploy en `deployments.json` (agregar una segunda entrada junto a la de Phase 2, no reemplazarla). Agregar una segunda entrada a `REGISTRIES` en `lib/config/tokens.ts` con label "Corte real (mini)" (la de Phase 2 queda como "Fixture sintética" o el label que Plan 01 ya le haya puesto).
  </action>
  <how-to-verify>
    1. Confirmar balance del publisher suficiente (mismo criterio D-02 de 02-CONTEXT.md, ~0.002 ETH adicionales si hace falta).
    2. Correr el deploy y confirmar el contrato visible en el explorer.
    3. Confirmar que `/status/twin-neobank` (Plan 01) ahora muestra un selector con dos registries.
  </how-to-verify>
  <resume-signal>Pegá las addresses deployadas y "deploy ok", o describí el problema (ej. "sin fondos suficientes")</resume-signal>
  <acceptance_criteria>
    `deployments.json` tiene dos entradas de Registry (fixture + mini) sin haber pisado la de Phase 2. `REGISTRIES` tiene dos entradas y `/status/twin-neobank` las muestra ambas.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const d=require('./deployments.json'); if(!Array.isArray(d.registries) || d.registries.length < 2) process.exit(1)"</automated>
  </verify>
  <done>Segundo Registry deployado y registrado, sin afectar el Registry de la fixture (fallback permanente).</done>
</task>

<task type="auto">
  <name>Task 3: Pipeline de proving + publish (cron + botón manual, solo si Task 2 = deploy ok)</name>
  <files>lib/sobrecito-mini/prove.ts, app/api/cuenta/corte-mini/route.ts, vercel.json, lib/db/schema.sql</files>
  <read_first>
    04-CONTEXT.md D-06, D-09. 04-02-PLAN.md (API de openings a extender para persistir filas reales por corte mini). 03-01-PLAN.md (`lib/db/schema.sql`, convención `CREATE TABLE IF NOT EXISTS` idempotente, D-02/D-03 de 03-CONTEXT.md — la tabla `openings` se agrega ahí con el mismo estilo). 03-CONTEXT.md D-13 (misma wallet publisher/vault M2, patrón de firma con viem).
  </read_first>
  <action>
    Si Task 2 no corrió, saltar este task. Si corrió: agregar a `lib/db/schema.sql` (el mismo archivo idempotente que crea 03-01-PLAN.md) un `CREATE TABLE IF NOT EXISTS openings (corte_id text, user_id text, balances jsonb, commitment text, created_at timestamptz default now(), primary key (corte_id, user_id))` (D-09). Crear `lib/sobrecito-mini/prove.ts` que exporta el ledger de Phase 3 a CSV en memoria, corre el circuito mini con `bb.js` (WASM, dentro de la función de Vercel, sin binarios nativos, timeout Fluid 300s) usando `circuits-mini/liabilities_batch_mini.json`, arma el `CutInput` (cL probado, cR/verdicts/coverageBps/attestationHash declarados con el mismo criterio de Phase 2) y publica al segundo Registry con `walletClient` de viem firmando con `PUBLISHER_PRIVATE_KEY` (env de Vercel, la misma wallet M2). Crear `app/api/cuenta/corte-mini/route.ts`: un handler compartido invocado por (a) el cron diario declarado en `vercel.json` y (b) un POST manual protegido con `CRON_SECRET` (mismo patrón que el cron de interés de Phase 3) para "correr corte ahora" en la demo en vivo. Cada corrida exitosa inserta una fila por usuario en `openings` (corte_id, user_id, balances, commitment) para que la verificación de inclusión (`app/api/cuenta/opening/route.ts` de Plan 02) sirva el corte real en vez del sintético cuando exista una fila para ese `corte_id`/DID.
  </action>
  <acceptance_criteria>
    El endpoint manual, invocado con el secret correcto, corre el pipeline completo y el nuevo corte aparece en `/status/twin-neobank` bajo el registry "Corte real (mini)". Sin el secret correcto, responde 401/403.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CRON_SECRET" app/api/cuenta/corte-mini/route.ts</automated>
  </verify>
  <done>SOL-06 cumplido (si se llegó hasta acá): pipeline real reemplaza la fixture, con la fixture como fallback permanente intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Función de Vercel ↔ `PUBLISHER_PRIVATE_KEY` | Misma clave que firma en Phase 2 (M2); firma el publish del corte mini además del de la fixture |
| Endpoint `/api/cuenta/corte-mini` (manual) | Dispara proving + publish real; debe estar protegido, no es de acceso público |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-07 | Tampering | `app/api/cuenta/corte-mini/route.ts` (POST manual) | mitigate | Protegido con `CRON_SECRET` en header, mismo patrón que el cron de interés de Phase 3; sin el secret, 401 |
| T-04-08 | Elevation of Privilege | `PUBLISHER_PRIVATE_KEY` reusada para el segundo Registry | accept | Riesgo ya aceptado en Phase 2 (single-sig, declarado en el disclosure de Phase 5); este plan no lo agrava, reusa la misma clave y el mismo criterio |
| T-04-09 | Denial of Service | Proving con bb.js en función serverless (memoria/timeout) | accept | Riesgo declarado en 04-CONTEXT.md "specifics"; el smoke test de Task 1 es la mitigación primaria (si no corre confiablemente en el smoke, el plan se abandona antes de exponerlo en producción) |
</threat_model>

<verification>
Task 1 gatea Task 2, que gatea Task 3. Con "smoke falló" en Task 1, el plan cierra ahí, documentado, sin bloquear Phase 4 ni Phase 5.
</verification>

<success_criteria>
Este plan es 100% opcional para el cierre de Phase 4: su éxito o abandono no aparece en los must_haves de Plans 01/02 ni en los success criteria agregados de la fase. Si llega hasta el final, SOL-06 queda cumplido con un pipeline real sobre el ledger de la demo, con la fixture de Phase 2 intacta como fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/04-solvencia-visible/04-03-SUMMARY.md` (documentar explícitamente en qué task se detuvo si el gate falló)
</output>

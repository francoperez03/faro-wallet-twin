---
phase: 03-modo-cuenta
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "01-02"]
files_modified:
  - lib/hooks/use-cuenta-mode.ts
  - components/mode-toggle.tsx
  - app/(tabs)/layout.tsx
  - app/(tabs)/cuenta/page.tsx
  - app/(tabs)/cuenta/pasar/page.tsx
  - app/(tabs)/cuenta/retirar/page.tsx
autonomous: false
requirements: [CTA-01, CTA-02, CTA-03, CTA-04]

must_haves:
  truths:
    - "User alterna entre modo Wallet y modo Cuenta con un toggle visible y persistente arriba de la app"
    - "User ve su saldo en Cuenta, el interés acumulado y la tasa actual en la Home de Cuenta"
    - "User pasa ARGt a su Cuenta y ve el estado pasar a 'acreditado' en segundos, sin recargar la página"
    - "User retira de su Cuenta a su embedded wallet en la chain elegida y ve el estado del retiro"
  artifacts:
    - path: "lib/hooks/use-cuenta-mode.ts"
      provides: "useCuentaMode(): { mode: 'wallet'|'cuenta', setMode }, persistido en localStorage (D-19)"
    - path: "components/mode-toggle.tsx"
      provides: "Toggle visual Wallet/Cuenta, montado en app/(tabs)/layout.tsx"
    - path: "app/(tabs)/cuenta/page.tsx"
      provides: "Home de Cuenta: saldo Display, interés acumulado, tasa (APY o '—'), CTAs Pasar a Cuenta / Retirar"
    - path: "app/(tabs)/cuenta/pasar/page.tsx"
      provides: "Transfer a la bóveda (switchChain a Arbitrum + transfer) con polling de sync-deposits hasta 'acreditado'"
    - path: "app/(tabs)/cuenta/retirar/page.tsx"
      provides: "Formulario monto + chain destino, llama /api/cuenta/withdraw, muestra estado pendiente/enviado/fallido con tx hash"
  key_links:
    - from: "app/(tabs)/layout.tsx"
      to: "lib/hooks/use-cuenta-mode.ts"
      via: "ModeToggle montado en el header del tab layout, controla qué ruta usa el tab Home"
      pattern: "useCuentaMode"
    - from: "app/(tabs)/cuenta/page.tsx"
      to: "app/api/cuenta/account/route.ts"
      via: "fetch con el access token de Privy para leer saldo + interés"
      pattern: "/api/cuenta/account"
    - from: "app/(tabs)/cuenta/pasar/page.tsx"
      to: "app/api/cuenta/sync-deposits/route.ts"
      via: "polling corto (~5s) tras el transfer hasta ver el movimiento nuevo"
      pattern: "/api/cuenta/sync-deposits"
    - from: "app/(tabs)/cuenta/retirar/page.tsx"
      to: "app/api/cuenta/withdraw/route.ts"
      via: "POST con amount y chain al confirmar"
      pattern: "/api/cuenta/withdraw"
---

<objective>
Pantallas del modo Cuenta: toggle persistente (CTA-01), Home con saldo/interés/tasa (CTA-03), Pasar a Cuenta con confirmación por polling (CTA-02) y Retirar con estado por chain (CTA-04). Reusa el flujo de transfer y el design system de Phase 1.

Purpose: la parte visible del modo Cuenta, la que se demuestra en vivo.
Output: 4 pantallas nuevas conectadas a los endpoints de 03-01/02/03, con el toggle integrado al shell de tabs existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@SPEC.md#7.4
@.planning/phases/03-modo-cuenta/03-CONTEXT.md
@.planning/phases/01-wallet-mode/01-UI-SPEC.md
@lib/config/tokens.ts
@lib/config/cuenta.ts
@app/(tabs)/layout.tsx
@app/(tabs)/enviar/page.tsx
</context>

<interfaces>
<!-- De 01-02 (Phase 1), ya en el repo. Reusar, no reescribir. -->

```typescript
// lib/hooks/use-token-balances.ts (Phase 1, 01-02)
export function useTokenBalances(): { perChain: Record<ChainKey, bigint>; total: bigint; isLoading: boolean };
```

`app/(tabs)/enviar/page.tsx` (Phase 1) ya resuelve el patrón completo: selector de chain, validación de address/monto, `switchChainAsync` + `writeContractAsync` con toast de estado. "Pasar a Cuenta" es ese mismo patrón con destino fijo (`OMNIBUS_VAULT_ADDRESS`) y chain fija (`DEPOSIT_CHAIN` = Arbitrum), sin selector de address ni de chain.

<!-- De 03-01/02/03, ya en el repo tras waves 1-2. -->

```typescript
// GET /api/cuenta/account -> { userId, walletAddress, argtBalance, boltBalance, interestAccrued } (strings, parsear a bigint)
// GET /api/cuenta/rate -> { apy: number | null, asOf?: string }
// POST /api/cuenta/sync-deposits -> { credited: number, myNewMovements: Movement[] }
// POST /api/cuenta/withdraw { amount: string, chain: ChainKey } -> { status: 'sent', txHash } | { status: 'failed', reason }
```

Todas las rutas de Cuenta requieren `Authorization: Bearer <access token de Privy>` (obtenido con `getAccessToken()` de `usePrivy()`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Toggle Wallet/Cuenta persistente (CTA-01)</name>
  <files>lib/hooks/use-cuenta-mode.ts, components/mode-toggle.tsx, app/(tabs)/layout.tsx</files>
  <read_first>
    03-CONTEXT.md decisión D-19 (localStorage, sin DB), 01-UI-SPEC.md "Color" (accent solo en CTAs/tab activo) y "Spacing Scale" (44px touch target).
  </read_first>
  <action>
    Crear `lib/hooks/use-cuenta-mode.ts`: hook `useCuentaMode()` que lee/escribe `localStorage['twin-mode']` (`'wallet' | 'cuenta'`, default `'wallet'`), expone `{ mode, setMode }`, sincroniza con `useState` + `useEffect` (SSR-safe: default `'wallet'` hasta montar en cliente). Crear `components/mode-toggle.tsx`: dos botones/segmented control "Wallet" / "Cuenta", 44px de alto, estado activo con accent `#2563EB`, `aria-label` en español. Montar `<ModeToggle>` en `app/(tabs)/layout.tsx` arriba de las tabs (persistente en todas las rutas). Cuando `mode === 'cuenta'`, el link del tab "Home" apunta a `/cuenta` en vez de `/home`; cuando `mode === 'wallet'`, apunta a `/home` (los demás tabs de Phase 1 quedan sin cambios, la Cuenta vive solo en `/cuenta/*`).
  </action>
  <acceptance_criteria>
    El toggle cambia de modo sin recargar, persiste tras un refresh (localStorage), y redirige el tab Home a `/cuenta` o `/home` según el modo activo.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CTA-01 cumplido: toggle visible, persistente, integrado al shell existente sin romper la navegación de Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Home de Cuenta y Pasar a Cuenta (CTA-02, CTA-03)</name>
  <files>app/(tabs)/cuenta/page.tsx, app/(tabs)/cuenta/pasar/page.tsx</files>
  <read_first>
    03-CONTEXT.md decisiones D-07, D-08, D-12 (switchChain a Arbitrum antes del transfer, polling corto ~5s, tasa con fallback "—"), 01-UI-SPEC.md "Typography" (Display 32px/600 reservado para el saldo hero) y "Copywriting Contract" (tono cuenta bancaria, sin jerga cripto).
  </read_first>
  <action>
    Crear `app/(tabs)/cuenta/page.tsx`: en mount, `fetch('/api/cuenta/account', { headers: { Authorization } })` con el access token de `usePrivy()`; muestra `argtBalance` en Display (formateado como pesos/ARGt, sin jerga), `interestAccrued` debajo, y `fetch('/api/cuenta/rate')` para la tasa ("X% anual" o "— · ver en Morpho" si `apy` es `null`, linkeando a Morpho como en Phase 1 D-07). CTAs "Pasar a Cuenta" y "Retirar" navegan a las subrutas. Crear `app/(tabs)/cuenta/pasar/page.tsx`: input de monto, validado contra `useTokenBalances().perChain.arbitrum` (`> 0` y `<= balance en Arbitrum`). Si el monto ingresado supera el balance en Arbitrum pero el usuario tiene ARGt en Base o Polygon (`useTokenBalances().perChain.base/polygon > 0`), no bloquear en seco: mostrar "No te alcanza en Arbitrum, pero tenés ARGt en {chain}. Bridgealo a Arbitrum primero" con link a `/mover` (mismo patrón que la pantalla del vault en Phase 1, D-07 de 01-CONTEXT.md). Botón que hace `switchChainAsync` a Arbitrum si hace falta y `writeContractAsync` `transfer(OMNIBUS_VAULT_ADDRESS, amount)` sobre `TOKENS.ARGt.addresses.arbitrum`. Tras la confirmación de la tx, poll `POST /api/cuenta/sync-deposits` cada ~5s (máx ~12 intentos) hasta ver el movimiento nuevo en `myNewMovements`; mientras tanto muestra "Confirmando..." y al aparecer muestra "Acreditado" (copy SPEC §7.4) con link al explorer. Si no llega en el tiempo máximo, muestra "Puede tardar unos minutos, va a aparecer en tu Cuenta" sin bloquear.
  </action>
  <acceptance_criteria>
    Home de Cuenta muestra saldo, interés y tasa reales del usuario logueado. Un transfer real a la bóveda desde Pasar a Cuenta termina mostrando "Acreditado" sin que el usuario recargue la página.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CTA-02 y CTA-03 cumplidos en UI: saldo/interés/tasa visibles, depósito confirmado por polling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Retirar (CTA-04) y verificación end-to-end del modo Cuenta</name>
  <files>app/(tabs)/cuenta/retirar/page.tsx</files>
  <read_first>
    03-CONTEXT.md decisiones D-14, D-16 (límite diario, estados pending/sent/failed), 01-UI-SPEC.md "Copywriting Contract" (errores "No pudimos completar la operación...", destructivo con confirmación).
  </read_first>
  <action>
    Crear `app/(tabs)/cuenta/retirar/page.tsx`: input de monto y selector de chain destino (3 chains), validación `> 0` y `<= argtBalance` de la Cuenta (no del balance on-chain). Al confirmar, `POST /api/cuenta/withdraw` con `{ amount, chain }`; muestra estado "Procesando" mientras espera la respuesta (síncrona: el endpoint devuelve `sent` o `failed`), y en éxito muestra el `tx_hash` con link al explorer; en fallo muestra el `reason` con el copy de error del UI-SPEC. Si el monto excede el límite diario restante, deshabilitar submit con mensaje explicando el límite de 1000 ARGt/día.
  </action>
  <how-to-verify>
    1. Con `npm run dev` y las envs de 03-01/02/03 seteadas, loguearse con Privy y confirmar que el toggle Wallet/Cuenta aparece arriba de la app y persiste tras refrescar.
    2. Cambiar a modo Cuenta: confirmar que la Home de Cuenta muestra saldo (real o "0" si es cuenta nueva), interés acumulado y tasa.
    3. Ir a "Pasar a Cuenta", transferir un monto chico de ARGt en Arbitrum, y confirmar que en menos de ~30s la pantalla muestra "Acreditado" y el saldo en la Home sube.
    4. Ir a "Retirar", pedir un retiro chico a una chain, y confirmar que el estado pasa a "enviado" con un `tx_hash` que resuelve en el explorer, y que el saldo en Cuenta baja.
    5. Intentar retirar más de 1000 ARGt en un día: confirmar que se bloquea con el mensaje del límite.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" o describí el problema (ej. "el polling de acreditación nunca encuentra el movimiento")</resume-signal>
  <acceptance_criteria>
    Flujo completo del modo Cuenta demostrable en vivo: toggle, depósito acreditado por evento, saldo/interés/tasa visibles, retiro firmado y confirmado, límite diario respetado.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cliente → `/api/cuenta/*` | Toda request lleva el access token de Privy; el cliente nunca manda `userId` ni balances, solo monto/chain |
| `localStorage['twin-mode']` | Preferencia de UI sin impacto de seguridad ni de negocio (D-19), no requiere sanitización especial más allá de un valor enum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-10 | Tampering | `app/(tabs)/cuenta/retirar/page.tsx` | mitigate | Validación de monto/límite en cliente es solo UX; la validación real (balance, límite diario, lock) vive en `lib/cuenta/withdrawals.ts` (03-02) y no puede bypassearse desde el cliente |
| T-03-11 | Information Disclosure | Home de Cuenta | accept | Saldo e interés del usuario logueado solo son visibles para él (token de Privy propio), sin listado de otros usuarios (D-18) |
</threat_model>

<verification>
`npm run build` sin errores. Flujo end-to-end (toggle → pasar a cuenta → acreditado → retirar → confirmado) verificado en el checkpoint con fondos reales.
</verification>

<success_criteria>
CTA-01, CTA-02, CTA-03 y CTA-04 cumplidos en UI, cableados a los endpoints reales de 03-01/02/03. Demo completa del modo Cuenta posible de punta a punta.
</success_criteria>

<output>
After completion, create `.planning/phases/03-modo-cuenta/03-04-SUMMARY.md`
</output>

---
phase: 05-ship
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - lib/config.ts
autonomous: false
requirements: [SHIP-01, SHIP-02, SHIP-03]

must_haves:
  truths:
    - "El nombre elegido por el usuario aparece en el título, el disclosure y el form de submission (rebrand solo de display)"
    - "El smoke test de los 9 puntos de D-10 corrió sobre la URL de producción"
    - "La submission (URL + nombre + mail) fue enviada antes de las 17:30"
  artifacts:
    - path: "lib/config.ts"
      provides: "PRODUCT_NAME actualizado al nombre elegido"
  key_links:
    - from: "lib/config.ts PRODUCT_NAME"
      to: "<title>, /disclosure, form de submission"
      via: "referencia directa a la constante, sin renombrar el slug de Vercel"
      pattern: "PRODUCT_NAME"
---

<objective>
Cerrar la fase: el usuario elige el nombre del producto (D-01, checkpoint), se corre el smoke test de producción en el orden de D-10, y se envía la submission (SHIP-03) antes de las 17:30. Sin trabajo técnico nuevo más allá del rebrand de display: esto es el trámite final.

Purpose: entregar antes del deadline duro (jueves 20/08 18:00) con evidencia de que el milestone funciona en producción.
Output: nombre elegido aplicado en display, checklist de smoke corrido y confirmado por el usuario, submission enviada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship/05-CONTEXT.md
@SPEC.md (§1 milestones de evaluación, §8 riesgos operativos)

Depende de 05-01 (footer/disclosure/HIDDEN_SECTIONS ya en producción) y 05-02 (env vars auditadas, cron registrado, URL de producción confirmada). Deadline duro: jueves 20/08 18:00. El smoke arranca a más tardar 16:00; la submission se envía a las 17:30 como máximo (D-11).
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Elegir nombre del producto (D-01)</name>
  <decision>Nombre de display del producto (working name actual: twin-neobank). El slug de Vercel y el repo NO se renombran (D-02) — solo cambian strings de UI: título, disclosure, nombre en el form de submission.</decision>
  <context>Tres candidatos propuestos en la discusión conjunta del 19-08, todos igual de válidos técnicamente:</context>
  <options>
    <option id="faro">
      <name>Faro</name>
      <pros>Metáfora directa con la solvencia visible on-chain (cualquiera puede mirar y verificar); corto, pronunciable, banca en español</pros>
      <cons>Menos conexión explícita con Twin/hackathon</cons>
    </option>
    <option id="vera">
      <name>Vera</name>
      <pros>De verdad/verificable; suena a fintech con nombre propio; funciona en inglés y español; conecta con la verificación de inclusión del cliente</pros>
      <cons>Menos ligado a la metáfora de solvencia que Faro</cons>
    </option>
    <option id="doble">
      <name>Doble</name>
      <pros>Guiño directo a Twin (gemelo) y al respaldo 1:1 de reservas probadas; el más ligado al hackathon</pros>
      <cons>Menos autoexplicativo para alguien sin contexto del hackathon</cons>
    </option>
  </options>
  <resume-signal>Responder: faro, vera, doble, u otro nombre a mano</resume-signal>
  <acceptance_criteria>
    - El usuario respondió con uno de: faro, vera, doble, u otro nombre explícito
    - La respuesta queda registrada como input para Task 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Aplicar el nombre elegido (display-only)</name>
  <read_first>
    - Leer `lib/config.ts` (agregado en 05-01) para confirmar dónde vive `PRODUCT_NAME`.
    - `grep -rn "PRODUCT_NAME\|twin-neobank" app/ lib/ --include="*.tsx" --include="*.ts"` para ubicar todos los usos.
  </read_first>
  <files>lib/config.ts</files>
  <action>
    Actualizar `export const PRODUCT_NAME` en `lib/config.ts` al nombre elegido en Task 1. Confirmar que el `<title>` del layout raíz, la página `/disclosure` y cualquier header de UI que muestre el nombre lean de esta constante (no un string hardcodeado aparte). NO renombrar el slug del proyecto Vercel, el repo, ni ninguna referencia a `twin-neobank` que sea infraestructura (nombre de proyecto en `vercel.json`, si lo hubiera).
  </action>
  <verify>
    <automated>grep -n "PRODUCT_NAME" lib/config.ts</automated>
  </verify>
  <done>PRODUCT_NAME refleja el nombre elegido; el slug de Vercel sigue siendo twin-neobank.</done>
  <acceptance_criteria>
    - lib/config.ts PRODUCT_NAME tiene el valor elegido en Task 1
    - El <title> del layout raíz, /disclosure y cualquier header de UI leen PRODUCT_NAME, sin strings hardcodeados aparte
    - El slug del proyecto Vercel y el repo siguen siendo twin-neobank
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke test de producción (orden D-10)</name>
  <what-built>App completa en producción: login, balances, transfer, vault, bridge, Cuenta, badge de solvencia, /status público, disclosure.</what-built>
  <how-to-verify>
    Sobre la URL de producción de Vercel, en este orden (arrancar a más tardar 16:00):
    1. Login con Privy (email) y confirmar que se crea la embedded wallet.
    2. Ver balance de ARGt por chain (Arbitrum, Base, Polygon) y el total.
    3. Transferir un monto chico de ARGt, confirmando el switchChain automático.
    4. Vault: depositar y ver la posición (retirar si el tiempo alcanza).
    5. Bridge: approve + bridge pagando fee, confirmar estado y balance de destino actualizado.
    6. Cuenta: pasar ARGt a Cuenta, ver acreditación, retirar.
    7. Ver el badge de solvencia en verde en el home de Cuenta.
    8. Abrir `/status/twin-neobank` sin login y confirmar que muestra el último corte.
    9. Confirmar que el footer de disclosure es visible y que `/disclosure` carga con el nombre nuevo.
    Si algún punto falla y son las 17:00 o más tarde: aplicar D-12, seteando `HIDDEN_SECTIONS` en `lib/config.ts` (prioridad de sacrificio: bonus BOLt → bridge → Cuenta+solvencia → vault; login+balances de M1 nunca se ocultan), redeployar, y continuar el checklist desde ahí.
  </how-to-verify>
  <resume-signal>Responder "approved" si los 9 puntos pasan, o listar qué falló (y si se aplicó HIDDEN_SECTIONS)</resume-signal>
  <acceptance_criteria>
    - Los 9 puntos del checklist D-10 se ejecutaron en orden sobre la URL de producción
    - El usuario respondió "approved" o listó explícitamente qué falló
    - Si algo falló a partir de las 17:00, HIDDEN_SECTIONS quedó seteado y redeployado antes de dar el checklist por cerrado
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Enviar la submission (SHIP-03)</name>
  <what-built>App verificada en producción, con nombre y disclosure finales.</what-built>
  <how-to-verify>
    Completar el form de submission del hackathon con: URL de producción de Vercel, nombre del proyecto (el elegido en Task 1), mail franco.perez03@gmail.com. Sin video (D-03). Enviar antes de las 17:30 (buffer de 30 min contra el deadline de las 18:00).
  </how-to-verify>
  <resume-signal>Responder "enviado" una vez confirmado el envío del form</resume-signal>
  <acceptance_criteria>
    - El form de submission fue enviado con URL de producción, nombre elegido en Task 1 y franco.perez03@gmail.com
    - El envío ocurrió antes de las 17:30
    - El usuario confirmó con "enviado"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Usuario → form de submission externo | Datos enviados a un servicio de terceros (form del hackathon) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-05-05 | Information Disclosure | Form de submission | accept | Solo URL pública, nombre y mail; ningún secreto ni dato de usuario real se envía |
</threat_model>

<verification>
- PRODUCT_NAME actualizado y visible en título, disclosure y footer.
- Los 9 puntos del smoke test de D-10 corridos y confirmados (o degradados explícitamente vía HIDDEN_SECTIONS con nota de qué se ocultó).
- Submission enviada antes de las 17:30, confirmada por el usuario.
</verification>

<success_criteria>
SHIP-01, SHIP-02 y SHIP-03 cerrados: la app vive en producción con el nombre final, el disclosure está probado en vivo, y la entrega fue enviada antes del deadline.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ship/05-03-SUMMARY.md`
</output>

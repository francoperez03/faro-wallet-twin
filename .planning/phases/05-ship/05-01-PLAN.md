---
phase: 05-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/disclosure/page.tsx
  - lib/config.ts
autonomous: true
requirements: [SHIP-02]

must_haves:
  truths:
    - "Cualquier página de la app muestra una línea de disclosure en el footer"
    - "El link del footer lleva a /disclosure con el texto completo"
    - "/disclosure explica qué está probado on-chain vs qué está declarado, y linkea a /status/twin-neobank"
    - "Existe un flag central para ocultar secciones rotas sin tocar cada pantalla"
  artifacts:
    - path: "app/disclosure/page.tsx"
      provides: "Página de disclosure completo (D-05)"
    - path: "app/layout.tsx"
      provides: "Footer persistente con línea corta + link"
    - path: "lib/config.ts"
      provides: "HIDDEN_SECTIONS y PRODUCT_NAME (working name twin-neobank)"
  key_links:
    - from: "app/layout.tsx"
      to: "app/disclosure/page.tsx"
      via: "<Link href=/disclosure>"
      pattern: "disclosure"
    - from: "nav/tab bar component"
      to: "lib/config.ts"
      via: "import HIDDEN_SECTIONS, filtra tabs antes de renderizar"
      pattern: "HIDDEN_SECTIONS"
---

<objective>
Agregar el disclosure in-app (SHIP-02) como footer persistente + página `/disclosure`, y dejar listo el mecanismo de feature flag (D-12) para poder ocultar secciones rotas el día de la demo sin shippear pantallas rotas.

Purpose: cubrir el requisito legal/de honestidad del hackathon (qué está probado vs declarado) y dar una salida de emergencia barata para el smoke test de Plan 03.
Output: `/disclosure` page, footer global, `lib/config.ts` con `HIDDEN_SECTIONS` y `PRODUCT_NAME`, nav gateado por el flag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship/05-CONTEXT.md
@.planning/phases/01-wallet-mode/01-UI-SPEC.md

Fase 1 no está ejecutada todavía al momento de planear esta fase (repo greenfield). Las rutas exactas de `app/layout.tsx`, el módulo de config único (D-10 de fase 1) y el componente de nav/tab bar son las convenciones estándar de Next.js App Router asumidas acá — el executor DEBE confirmarlas con `read_first` antes de escribir, y ajustar si fase 1/3/4 usaron otro nombre de archivo.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config module — PRODUCT_NAME y HIDDEN_SECTIONS</name>
  <read_first>
    - Buscar el módulo de config único creado en Phase 1 (D-10, registry de tokens): `grep -rn "ARGt\|tokenRegistry\|TOKEN" --include="*.ts" lib/ src/ app/ 2>/dev/null | grep -i config`. Si existe, agregar ahí (no crear un segundo módulo de config). Si no existe todavía (fase 1 no ejecutada), crear `lib/config.ts`.
  </read_first>
  <files>lib/config.ts</files>
  <action>
    En el módulo de config único, agregar dos exports:
    - `export const PRODUCT_NAME = "twin-neobank"` — working name (D-02). Comentario arriba: se reemplaza por Faro/Vera/Doble solo acá cuando el usuario elija (Plan 03, Task 1); nunca renombrar el slug de Vercel.
    - `export const HIDDEN_SECTIONS: string[] = []` — array vacío por default. Comentario con la prioridad de sacrificio de D-12 (de último a primero): `'bolt'`, `'bridge'`, `'cuenta'`, `'vault'`. `'m1'` (login+balances) nunca entra acá.
    No implementar la lógica de ocultamiento acá todavía, solo declarar las constantes (Task 2 las consume).
  </action>
  <verify>
    <automated>grep -n "PRODUCT_NAME\|HIDDEN_SECTIONS" lib/config.ts</automated>
  </verify>
  <done>lib/config.ts exporta PRODUCT_NAME="twin-neobank" y HIDDEN_SECTIONS=[] con el comentario de prioridad de sacrificio.</done>
  <acceptance_criteria>
    - lib/config.ts exporta PRODUCT_NAME con valor "twin-neobank"
    - lib/config.ts exporta HIDDEN_SECTIONS como array vacío
    - Existe un comentario documentando la prioridad de sacrificio de D-12 (bolt, bridge, cuenta, vault)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Footer global + gateo de nav por HIDDEN_SECTIONS</name>
  <read_first>
    - Leer el root layout (`app/layout.tsx` o equivalente App Router) para ver el árbol actual y dónde entra un footer global.
    - Leer el componente de nav/tab bar (creado en fase 1, lista Home/Vault/Bridge/Cuenta) para ver cómo arma la lista de tabs.
  </read_first>
  <files>app/layout.tsx, [componente de nav/tab bar existente]</files>
  <action>
    1. Agregar un footer simple al layout raíz, visible en toda la app (fuera del contenido scrolleable, estilo Label 14px/zinc-500 por 01-UI-SPEC.md), con una línea corta tipo "PoC de hackathon, no auditado" y un link a `/disclosure` con el estilo de link accent (#2563EB) de la spec de UI.
    2. En el componente de nav/tab bar, filtrar la lista de tabs contra `HIDDEN_SECTIONS` de `lib/config.ts` antes de renderizar (ej. `tabs.filter(t => !HIDDEN_SECTIONS.includes(t.id))`). Este es el único choke point que hace falta tocar: alcanza con ocultar el tab, no hace falta gatear cada página individual.
    No tocar contenido de las páginas individuales (Bridge, Cuenta, Vault): con ocultar el tab de nav alcanza para el smoke test de emergencia.
  </action>
  <verify>
    <automated>grep -n "HIDDEN_SECTIONS" app/layout.tsx [componente-nav] 2>/dev/null; grep -n "/disclosure" app/layout.tsx</automated>
  </verify>
  <done>Footer visible en todas las páginas con link a /disclosure; nav filtra tabs según HIDDEN_SECTIONS.</done>
  <acceptance_criteria>
    - El footer aparece en el layout raíz y es visible en cualquier ruta de la app
    - El footer contiene un link a /disclosure
    - El componente de nav importa HIDDEN_SECTIONS y filtra la lista de tabs antes de renderizar
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Página /disclosure</name>
  <read_first>
    - Leer 05-CONTEXT.md D-05 para el contenido locked exacto.
    - Confirmar la URL de `/status/twin-neobank` (fase 4) antes de linkearla; si fase 4 usó otro slug, ajustar el link.
  </read_first>
  <files>app/disclosure/page.tsx</files>
  <action>
    Crear la página `/disclosure` (App Router, server component simple, sin data fetching) con el contenido locked de D-05, en español, sin la construcción "no es X, es Y" y sin guiones largos:
    - PoC no auditado, construido para el hackathon "Twin your Neobank".
    - Qué está probado on-chain (cL, pasivos) vs qué está declarado (cR, verdicts, coverage, vía declaredMask), con link a `/status/twin-neobank` como evidencia.
    - La clave de la bóveda vive en el server como límite de esta demo (sin HSM ni multisig).
    - Texto legal de Twin: las Twin Stablecoins son instrumentos de pago respaldados por reservas, no productos de inversión.
    Layout simple: título (Heading 20px/600), párrafos (Body 16px/400), sin componentes nuevos de shadcn si un `<p>` alcanza.
  </action>
  <verify>
    <automated>grep -q "no auditado" app/disclosure/page.tsx && grep -q "declaredMask" app/disclosure/page.tsx && grep -qE "clave.*bóveda|servidor" app/disclosure/page.tsx && grep -q "instrumentos de pago" app/disclosure/page.tsx</automated>
  </verify>
  <done>/disclosure renderiza el texto completo de D-05 y linkea a /status/twin-neobank.</done>
  <acceptance_criteria>
    - /disclosure incluye las 4 secciones de D-05: PoC no auditado, qué está probado vs declarado (declaredMask), clave de bóveda en el servidor, texto legal de Twin
    - /disclosure linkea a /status/twin-neobank
    - La copy no usa "no es X, es Y" ni guiones largos
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Público → /disclosure | Página pública, sin login, sin input de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-05-01 | Information Disclosure | lib/config.ts | accept | Solo constantes públicas (nombre de producto, lista de secciones ocultas); nunca secretos, cubierto además por el checklist de Plan 02 |
| T-05-02 | Tampering | app/disclosure/page.tsx | accept | Server component estático, sin input de usuario ni mutación |
</threat_model>

<verification>
- `/disclosure` accesible sin login y con el contenido de D-05.
- Footer visible en al menos dos rutas distintas de la app (ej. home y una pantalla interna).
- Con `HIDDEN_SECTIONS = ['bridge']` seteado manualmente, el tab de Bridge desaparece del nav sin romper el resto de la app (probar y revertir a `[]` antes de terminar).
</verification>

<success_criteria>
Footer + /disclosure cubren SHIP-02 completo; HIDDEN_SECTIONS queda operativo para Plan 03 (contingencia D-12) sin haber tenido que usarse todavía.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ship/05-01-SUMMARY.md`
</output>

---
phase: 05-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
autonomous: true
requirements: [SHIP-01]

must_haves:
  truths:
    - "La app responde en la URL pública de Vercel"
    - "Ninguna env var secreta tiene prefijo NEXT_PUBLIC_"
    - "El cron de interés diario está registrado y corre en el plan hobby"
    - "El endpoint de trigger manual del cron sigue disponible como respaldo"
  artifacts:
    - path: "vercel.json"
      provides: "Definición del cron diario (crons)"
  key_links:
    - from: "vercel.json"
      to: "endpoint de interés (Phase 3, CTA-06)"
      via: "path del cron"
      pattern: "crons"
---

<objective>
Verificar y cerrar la infraestructura de deploy (SHIP-01): env vars correctas en Vercel (nada secreto con `NEXT_PUBLIC_`), cron de interés diario registrado, y confirmar que la URL de producción responde. El proyecto Vercel ya existe (deployado desde fase 1); acá no hay trabajo de feature nuevo, solo verificación y el cron.

Purpose: que el ship final sea trámite, no debugging de infra a último momento.
Output: `vercel.json` con `crons`, checklist de env vars verificado, URL de producción confirmada viva.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship/05-CONTEXT.md

Proyecto Vercel: cuenta `francoperez03s-projects`, plan hobby (D-07). Requiere `vercel` CLI logueado (ya debería estarlo desde el deploy de fase 1; si `vercel whoami` falla, es un auth gate que se resuelve en el momento, no algo a preplanear).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar env vars en Vercel (Production)</name>
  <read_first>
    - 05-CONTEXT.md D-08 para la lista exacta de env vars esperadas (públicas vs solo-server).
  </read_first>
  <files>(ninguno — verificación contra el dashboard/CLI de Vercel)</files>
  <action>
    Correr `vercel env ls production` (o `vercel env ls` filtrando Production) y confirmar contra D-08:
    - Públicas (`NEXT_PUBLIC_`): `NEXT_PUBLIC_PRIVY_APP_ID` presente; RPCs override solo si fase 1 los configuró por env.
    - Solo server, jamás `NEXT_PUBLIC_`: `DATABASE_URL`, `VAULT_PRIVATE_KEY`, `SOBRECITO_MASTER_HEX` presentes y SIN el prefijo `NEXT_PUBLIC_`.
    Si falta alguna o alguna secreta tiene el prefijo `NEXT_PUBLIC_` por error, corregir con `vercel env add` / `vercel env rm` según corresponda (nunca imprimir el valor de una env var secreta en el output).
  </action>
  <verify>
    <automated>vercel env ls production | grep -q NEXT_PUBLIC_PRIVY_APP_ID && ! (vercel env ls production | grep "NEXT_PUBLIC_" | grep -qE "VAULT_PRIVATE_KEY|SOBRECITO_MASTER_HEX|DATABASE_URL")</automated>
  </verify>
  <done>Las 3 env vars server-only están presentes sin prefijo NEXT_PUBLIC_; NEXT_PUBLIC_PRIVY_APP_ID está presente.</done>
  <acceptance_criteria>
    - vercel env ls production lista NEXT_PUBLIC_PRIVY_APP_ID
    - Ninguna de VAULT_PRIVATE_KEY, SOBRECITO_MASTER_HEX, DATABASE_URL aparece con prefijo NEXT_PUBLIC_
    - El comando de verify falla si alguna de esas tres aparece con ese prefijo
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Cron de interés en vercel.json + deploy + verificación de URL</name>
  <read_first>
    - Buscar el endpoint de trigger de interés de fase 3 (CTA-06): `grep -rn "interest\|cron" app/api 2>/dev/null`.
  </read_first>
  <files>vercel.json</files>
  <action>
    1. Crear o editar `vercel.json` en la raíz agregando:
       ```json
       {
         "crons": [
           { "path": "/api/[endpoint-de-interes-de-fase-3]", "schedule": "0 3 * * *" }
         ]
       }
       ```
       Una sola entrada diaria (el plan hobby no permite más de una ejecución diaria por cron). El endpoint de trigger manual de CTA-06 queda sin tocar, como respaldo.
    2. Deployar a producción (`vercel --prod` o push si el proyecto ya tiene auto-deploy configurado desde git).
    3. Confirmar que la URL de producción responde 200 en la home.
  </action>
  <verify>
    <automated>cat vercel.json | grep -A3 crons; curl -s -o /dev/null -w "%{http_code}" "$(vercel inspect --prod 2>/dev/null | grep -o 'https://[^ ]*' | head -1)"</automated>
  </verify>
  <done>vercel.json tiene un cron diario válido para el plan hobby; la URL de producción responde 200.</done>
  <acceptance_criteria>
    - vercel.json contiene un array crons con exactamente una entrada
    - La entrada del cron apunta al endpoint de interés de fase 3 (CTA-06)
    - curl a la URL de producción devuelve 200
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Vercel dashboard → runtime | Env vars server-only nunca deben llegar al bundle de cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-05-03 | Information Disclosure | Env vars de Vercel | mitigate | Task 1 audita explícitamente que DATABASE_URL, VAULT_PRIVATE_KEY, SOBRECITO_MASTER_HEX no tengan prefijo NEXT_PUBLIC_ antes del smoke final |
| T-05-04 | Denial of Service | Cron de interés | accept | Un solo cron diario en plan hobby; el endpoint de trigger manual de CTA-06 cubre el caso de que el cron no corra a tiempo |
</threat_model>

<verification>
- `vercel env ls production` no muestra ninguna de las 3 env vars server-only con prefijo `NEXT_PUBLIC_`.
- `vercel.json` válido (JSON parseable) con un cron diario.
- La URL de producción responde 200.
</verification>

<success_criteria>
SHIP-01 cerrado: URL pública viva, env vars auditadas, cron de interés registrado y con respaldo manual.
</success_criteria>

<output>
After completion, create `.planning/phases/05-ship/05-02-SUMMARY.md`
</output>

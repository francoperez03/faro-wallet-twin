---
phase: 02-sobrecito-registry
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/Publish.s.sol
  - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/broadcast/Publish.s.sol/42161/run-latest.json
  - /Users/francoperez/repos/twin/deployments.json
  - /Users/francoperez/repos/twin/lib/config/tokens.ts
  - /Users/francoperez/repos/twin/.env.example
autonomous: true
requirements: [SOL-02]

must_haves:
  truths:
    - "Un publish con la prueba de la fixture full_cut/root es aceptado on-chain en el Registry real y queda visible en Arbiscan"
    - "Un publish con la prueba adulterada revierte (ProofRejected), demostrado contra el contrato real desplegado"
    - "El repo twin tiene las addresses del deploy (Registry, verifier), tx hash y key_hash registrados, en el schema que Phase 4 ya espera, sin tipear nada a mano"
  artifacts:
    - path: "/Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/Publish.s.sol"
      provides: "Único código nuevo de la fase (D-10): arma CutInput + calldata y llama publish(), reusable por el pipeline real de Phase 4"
    - path: "/Users/francoperez/repos/twin/deployments.json"
      provides: "{ registries: [{ label, address, verifierAddress, chainId, deployTx, keyHash, explorer }] } — schema que 04-01-PLAN.md y 04-03-PLAN.md ya consumen, un solo entry con label 'Fixture sintética' (Phase 4 le agrega un segundo)"
    - path: "/Users/francoperez/repos/twin/lib/config/tokens.ts"
      provides: "REGISTRIES: {label, address, chainId}[] leído de NEXT_PUBLIC_REGISTRY_1_*, mismo módulo que 04-01-PLAN.md espera extender"
    - path: "/Users/francoperez/repos/twin/.env.example"
      provides: "NEXT_PUBLIC_REGISTRY_1_LABEL / _ADDRESS / _CHAIN_ID (D-08c, mismo naming que 04-01 usa para leer REGISTRIES)"
  key_links:
    - from: "Publish.s.sol"
      to: "circuits/fixtures/full_cut/root/proof y /public_inputs"
      via: "vm.readFileBinary, mismo patrón que test/Fixture.sol"
      pattern: "readFileBinary.*full_cut/root"
    - from: "Publish.s.sol (modo tamper)"
      to: "SobrecitoRegistry.publish"
      via: "eth_call simulation (sin --broadcast) con proof adulterada"
      pattern: "ProofRejected"
    - from: "twin/deployments.json (registries[0])"
      to: "Sobrecito ops/deployments.json"
      via: "copia literal de campos (chainId, sobrecito_registry, honk_verifier, registry_deploy_tx, key_hash, explorer)"
      pattern: "\"registries\"\\s*:\\s*\\["
    - from: "twin/lib/config/tokens.ts (REGISTRIES)"
      to: "twin/.env.example (NEXT_PUBLIC_REGISTRY_1_*)"
      via: "lectura de env en runtime, con deployments.json como fallback"
      pattern: "NEXT_PUBLIC_REGISTRY_1_ADDRESS"
---

<objective>
Escribir el único código nuevo de la fase (`Publish.s.sol` en el repo Sobrecito, D-10), publicar el corte de la fixture contra el Registry real desplegado en Plan 01, demostrar el caso negativo por simulación `eth_call` sin quemar gas (D-11), y dejar en el repo twin el registro de addresses en el schema (`deployments.json` con `registries[]`, `lib/config/tokens.ts` con `REGISTRIES`) que Phase 4 ya escribió sus consumidores para leer.

Purpose: cumple SOL-02 (publish aceptado + publish inválido revierte) y deja el handoff listo, en el formato correcto, para que Phase 4 lea el Registry desde la app sin tener que adaptar nada.
Output: `Publish.s.sol`, un `CutPublished` real en Arbiscan, evidencia del revert negativo, y `deployments.json` + `lib/config/tokens.ts` + `.env.example` en twin con el schema de `registries[]`/`REGISTRIES`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/francoperez/repos/twin/.planning/phases/02-sobrecito-registry/02-CONTEXT.md
@/Users/francoperez/repos/twin/.planning/phases/02-sobrecito-registry/02-01-PLAN.md
@/Users/francoperez/repos/twin/.planning/phases/04-solvencia-visible/04-01-PLAN.md
@/Users/francoperez/repos/twin/.planning/phases/04-solvencia-visible/04-03-PLAN.md

# Canónicos del repo Sobrecito (solo lectura salvo Publish.s.sol, que es el único archivo nuevo)
@/Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/Deploy.s.sol
@/Users/francoperez/repos/job/Sobre/sobrecito/contracts/test/Fixture.sol
@/Users/francoperez/repos/job/Sobre/sobrecito/contracts/test/SobrecitoRegistry.t.sol
@/Users/francoperez/repos/job/Sobre/sobrecito/contracts/src/SobrecitoRegistry.sol
@/Users/francoperez/repos/job/Sobre/sobrecito/circuits/fixtures/manifest.json

<interfaces>
`SobrecitoRegistry.publish(CutInput calldata inp, bytes calldata proof, bytes32[] calldata publicInputs) external`

```solidity
struct CutInput {
    bytes32 corteId;
    bytes32 cL;             // PROBADO: debe igualar publicInputs[1]
    bytes32 cR;              // DECLARADO
    uint64 blockB;
    uint8[] verdicts;        // DECLARADO
    uint16[] coverageBps;    // DECLARADO
    bytes32 attestationHash; // DECLARADO
}
```

Errores relevantes: `NotPublisher()`, `CutExists(bytes32)`, `KeyHashMismatch(bytes32,bytes32)`, `CLMismatch(bytes32,bytes32)`, `ProofRejected(bytes32)`.

Valores del caso positivo, copiados de `SobrecitoRegistryTest._validInput()` (test/SobrecitoRegistry.t.sol):
- `corteId`: `keccak256("fixture-reducida")`
- `cL`: `pi[1]` (segundo elemento de `public_inputs`, cargado del archivo real — NUNCA hardcodeado, se lee del artefacto)
- `cR`: `bytes32(uint256(0xC0FFEE))`
- `verdicts`: `[1,1,1,1]` (4 tokens, `manifest.tree.t == 4`)
- `coverageBps`: `[10000,9800,10000]`
- `attestationHash`: `keccak256("attestation-sintetica")`
- `blockB`: en el test es un valor fijo de fixture (`8_000_000`); en `Publish.s.sol` usar `uint64(block.number)` del bloque real al momento del publish (D-05 ya establece el patrón "nunca copiar un valor fijo cuando hay un dato real disponible en runtime").

Carga de proof/public_inputs (patrón de `FixtureLoader` en `test/Fixture.sol`, reusar tal cual):
```solidity
bytes memory proof = vm.readFileBinary("../circuits/fixtures/full_cut/root/proof"); // 10688 bytes
// public_inputs: leer raw bytes, trocear cada 32 bytes en bytes32[]
```
Paths relativos a `contracts/` (raíz del proyecto Foundry); `foundry.toml` ya autoriza `fs_permissions` de lectura a `../circuits/fixtures`.

Caso negativo D-07a de referencia (`test_corrupt_proof_reverts`): `flipByte(proof, 100)` produce una proof adulterada que revierte con `ProofRejected`.

Addresses del Registry deployado: leer de `/Users/francoperez/repos/job/Sobre/sobrecito/ops/deployments.json` (generado en Plan 01), campos `sobrecito_registry`, `honk_verifier`, `chainId`, `key_hash`, `verifier_deploy_tx`, `registry_deploy_tx`, `explorer`.

Higiene de clave: en Task 2 aplica la misma regla dura que Plan 01 Task 3 — el grep de verificación de que la clave no quedó grabada en ningún artefacto usa `grep -f` con pattern-file por process substitution, nunca `grep -e "$(cat ...)"` (la clave jamás en argv).

**Schema de handoff que Phase 4 ya espera consumir (bloqueante, leído de 04-01-PLAN.md y 04-03-PLAN.md):**

`deployments.json` (raíz de twin):
```json
{
  "registries": [
    {
      "label": "Fixture sintética",
      "address": "0x...",
      "verifierAddress": "0x...",
      "chainId": 42161,
      "deployTx": "0x...",
      "keyHash": "0x...",
      "explorer": "https://arbiscan.io/address/0x..."
    }
  ]
}
```
Array, no objeto plano: 04-03-PLAN.md le agrega una segunda entrada ("Corte real (mini)") sin pisar esta. `address` = `sobrecito_registry` de Sobrecito, `verifierAddress` = `honk_verifier`, `deployTx` = `registry_deploy_tx`.

`lib/config/tokens.ts` (raíz de twin, self-contained si Phase 1 todavía no lo creó): exporta `REGISTRIES: {label: string, address: `0x${string}`, chainId: number}[]`, poblado desde env `NEXT_PUBLIC_REGISTRY_1_LABEL` / `NEXT_PUBLIC_REGISTRY_1_ADDRESS` / `NEXT_PUBLIC_REGISTRY_1_CHAIN_ID` (mismo naming que 04-01-PLAN.md usa: `NEXT_PUBLIC_REGISTRY_1_...`, `NEXT_PUBLIC_REGISTRY_2_...` opcional para el mini de Phase 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir Publish.s.sol (D-10)</name>
  <files>/Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/Publish.s.sol</files>
  <read_first>
    - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/Deploy.s.sol (patrón de script: console2 logging, autoverificación, cero secretos)
    - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/test/Fixture.sol (carga de proof/public_inputs)
    - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/test/SobrecitoRegistry.t.sol (`_validInput()`, `flipByte`)
  </read_first>
  <action>
    Crear `contracts/script/Publish.s.sol`, un `forge-std/Script.sol` mínimo que:
    1. Lee `REGISTRY_ADDRESS` de env (`vm.envAddress("REGISTRY_ADDRESS")`) — la address real de Plan 01, nunca hardcodeada.
    2. Carga `proof` y `publicInputs` desde `../circuits/fixtures/full_cut/root/{proof,public_inputs}` con el mismo patrón de `FixtureLoader` (readFileBinary + trocear en `bytes32[]` de 32 bytes).
    3. Lee un flag opcional `TAMPER_PROOF` (`vm.envOr("TAMPER_PROOF", false)`): si es `true`, aplica el mismo XOR de 1 bit sobre el byte 100 del proof cargado que usa `flipByte`/`test_corrupt_proof_reverts`, para reusar el mismo script en el caso negativo sin duplicar código.
    4. Lee un `corteId` configurable por env (`vm.envOr("CORTE_ID", string("fixture-reducida"))`, hasheado con `keccak256(bytes(...))`) para poder correr el caso negativo con un id distinto al del publish real y así observar `ProofRejected` en vez de `CutExists`.
    5. Arma el `CutInput` con los valores de `_validInput()` documentados arriba, con `cL = publicInputs[1]` y `blockB = uint64(block.number)`.
    6. Llama `SobrecitoRegistry(REGISTRY_ADDRESS).publish(inp, proof, publicInputs)` dentro de `vm.startBroadcast()`/`vm.stopBroadcast()`; el mismo `run()` sirve como simulación `eth_call` cuando el caller omite `--broadcast` (comportamiento nativo de `forge script`, sin código extra).
    7. `console2.log` de provenance: `corteId`, `cL`, `blockB`, `tamper_proof` (bool), mismo estilo que `Deploy.s.sol`.
    Respetar `foundry.toml` tal cual (solc 0.8.28, `via_ir = false`, fs_permissions existentes). Cero secretos en este archivo.
  </action>
  <verify>
    <automated>cd /Users/francoperez/repos/job/Sobre/sobrecito/contracts && forge build</automated>
  </verify>
  <done>`Publish.s.sol` compila, no requiere cambios a `foundry.toml`, y reusa `REGISTRY_ADDRESS` + `TAMPER_PROOF` + `CORTE_ID` por env sin ningún valor hardcodeado de deploy.</done>
  <acceptance_criteria>El script existe, compila, y su `run()` sirve tanto para el publish real (Task 2, `--broadcast`) como para la simulación negativa (Task 2, sin `--broadcast`, `TAMPER_PROOF=true`, `CORTE_ID` distinto).</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Publish real de la fixture + caso negativo simulado (D-09, D-11)</name>
  <files>/Users/francoperez/repos/job/Sobre/sobrecito/contracts/broadcast/Publish.s.sol/42161/run-latest.json</files>
  <read_first>02-01-PLAN.md (must_haves.artifacts, addresses del deploy)</read_first>
  <action>
    Desde `/Users/francoperez/repos/job/Sobre/sobrecito/contracts`, con `REGISTRY_ADDRESS` leído de `../ops/deployments.json` (campo `sobrecito_registry`):

    1. Positivo (D-09), con broadcast real:
       `REGISTRY_ADDRESS=<addr> forge script script/Publish.s.sol:Publish --rpc-url https://arbitrum-one-rpc.publicnode.com --private-key "$(cat ~/.wakeup-m2-arb1.key)" --broadcast`
       Misma regla dura de higiene de clave que en Plan 01 Task 3: nunca imprimir, loggear ni commitear la clave. Al terminar, correr el mismo grep vía pattern-file (nunca la clave en argv):
       `grep -arF -f <(sed 's/^0x//' ~/.wakeup-m2-arb1.key) /Users/francoperez/repos/job/Sobre/sobrecito/contracts/broadcast/Publish.s.sol/` — debe dar 0 matches.
       Confirmar en consola: sin revert, `CutPublished` emitido; anotar el tx hash.

    2. Negativo (D-11), simulación sin broadcast (no gasta gas real):
       `REGISTRY_ADDRESS=<addr> TAMPER_PROOF=true CORTE_ID=fixture-reducida-tamper-check forge script script/Publish.s.sol:Publish --rpc-url https://arbitrum-one-rpc.publicnode.com --private-key "$(cat ~/.wakeup-m2-arb1.key)"` (sin `--broadcast`)
       Usar un `CORTE_ID` distinto al del paso 1 evita que el revert observado sea `CutExists` en vez de `ProofRejected`.
       Confirmar que la simulación revierte con el selector de `ProofRejected(bytes32)` (forge script imprime el revert reason/selector aunque no haya broadcast).

    3. Verificar en Arbiscan (best-effort, D-07): la tx del paso 1 visible en `https://arbiscan.io/tx/<hash>` con sus logs (aunque el source no esté verificado, la tx y los logs se ven igual).

    4. Dejar constancia en el summary de los 22 tests de Foundry (positivo + 5 negativos, corridos en Plan 01 Task 2) como respaldo documentado adicional al negativo real de este paso.
  </action>
  <verify>
    <automated>TX=$(python3 -c "import json;print(json.load(open('/Users/francoperez/repos/job/Sobre/sobrecito/contracts/broadcast/Publish.s.sol/42161/run-latest.json'))['transactions'][0]['hash'])") && cast receipt "$TX" --rpc-url https://arbitrum-one-rpc.publicnode.com | grep -i "status.*1 (success)"</automated>
  </verify>
  <done>El publish real tiene receipt con status success on-chain en Arbitrum One y es visible en Arbiscan; la simulación con proof adulterada revierte con `ProofRejected`, capturado en la consola de `forge script`; grep de higiene de clave da 0 matches.</done>
  <acceptance_criteria>SOL-02 satisfecho: publish con la proof de la fixture aceptado on-chain y visible en el explorer; publish inválido (proof adulterada) revierte contra el contrato real desplegado.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Handoff de addresses al repo twin en el schema de Phase 4 (D-08)</name>
  <files>
    - /Users/francoperez/repos/twin/deployments.json
    - /Users/francoperez/repos/twin/lib/config/tokens.ts
    - /Users/francoperez/repos/twin/.env.example
  </files>
  <read_first>
    - /Users/francoperez/repos/job/Sobre/sobrecito/ops/deployments.json (fuente única de verdad, generado en Plan 01)
    - 04-01-PLAN.md Task 1 (forma exacta de `REGISTRIES` y de dónde lee `lib/config/tokens.ts`)
    - 04-03-PLAN.md Task 2 (forma exacta de `deployments.json.registries[]`, segunda entrada que Phase 4 agrega)
  </read_first>
  <action>
    En el repo twin (`/Users/francoperez/repos/twin`), sin tipear ninguna address a mano (copiar literal de `ops/deployments.json` de Sobrecito), en el schema que Phase 4 ya escribió sus consumidores para leer (BLOQUEANTE: no usar un schema propio):

    1. `deployments.json` en la raíz de twin (D-08a): `{ "registries": [ { "label": "Fixture sintética", "address": <sobrecito_registry>, "verifierAddress": <honk_verifier>, "chainId": 42161, "deployTx": <registry_deploy_tx>, "keyHash": <key_hash>, "explorer": <explorer link a Arbiscan> } ] }`. Un array de un solo elemento — Phase 4 (04-03) le agrega una segunda entrada más adelante, nunca reemplazar el array por un objeto plano.

    2. `lib/config/tokens.ts` en la raíz de twin (D-08b): si el archivo no existe todavía (Phase 1 corre en paralelo y puede no haberlo creado), crearlo self-contained con un comentario "Cuando Phase 1 lo cree con otro contenido, mergear REGISTRIES acá; si ya existe, solo agregar este export sin tocar el resto del archivo." Exporta `REGISTRIES: {label: string, address: `0x${string}`, chainId: number}[]` con una sola entrada, leída de `process.env.NEXT_PUBLIC_REGISTRY_1_LABEL` / `NEXT_PUBLIC_REGISTRY_1_ADDRESS` / `NEXT_PUBLIC_REGISTRY_1_CHAIN_ID`, con fallback a los valores de `deployments.json` de la raíz si las env vars no están seteadas. Si el archivo ya existe (Phase 1 corrió primero), agregar el export sin pisar lo demás.

    3. `.env.example` en la raíz de twin (D-08c): agregar (o crear si no existe) `NEXT_PUBLIC_REGISTRY_1_LABEL=Fixture sintética`, `NEXT_PUBLIC_REGISTRY_1_ADDRESS=0x...`, `NEXT_PUBLIC_REGISTRY_1_CHAIN_ID=42161` con las addresses reales como valor de ejemplo (son públicas, no secretas) y un comentario apuntando a `deployments.json`.
  </action>
  <verify>
    <automated>test -f /Users/francoperez/repos/twin/deployments.json && test -f /Users/francoperez/repos/twin/lib/config/tokens.ts && test -f /Users/francoperez/repos/twin/.env.example && python3 -c "import json; d=json.load(open('/Users/francoperez/repos/twin/deployments.json')); assert isinstance(d['registries'], list) and len(d['registries'])==1; r=d['registries'][0]; assert r['chainId']==42161; assert r['address'].startswith('0x'); assert r['verifierAddress'].startswith('0x')"</automated>
  </verify>
  <done>Los tres archivos existen en twin, con `deployments.json.registries[0]` y `lib/config/tokens.ts` `REGISTRIES` copiados 1:1 del `ops/deployments.json` de Sobrecito (nunca tipeados a mano), en el schema exacto que `04-01-PLAN.md` y `04-03-PLAN.md` ya esperan leer/extender.</done>
  <acceptance_criteria>`deployments.json` (con `registries[]`), `lib/config/tokens.ts` (con `REGISTRIES`) y `.env.example` en el repo twin reflejan exactamente el deploy y el publish reales de Plan 01/Task 2, sin discrepancias con la chain, y sin romper lo que Phase 4 espera consumir.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Filesystem clave -> proceso Foundry | Igual que Plan 01: `~/.wakeup-m2-arb1.key` cruza a un flag de proceso vía command substitution, ahora en `Publish.s.sol` |
| `Publish.s.sol` -> `SobrecitoRegistry.publish` | Calldata armada por script cruza a un contrato que gatea con `verify()` antes de aceptar cualquier campo declarado |
| Repo twin <- `ops/deployments.json` de Sobrecito | Datos cruzan de un repo externo a este, deben copiarse literales en el schema `registries[]`, nunca reinterpretados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-02-06 | Information Disclosure | Task 2, carga de la clave en `Publish.s.sol` | mitigate | Misma regla de Plan 01: command substitution inline, jamás `echo`/logs/env persistente; grep de higiene vía pattern-file (`grep -f <(...)`, nunca la clave en argv) sobre `broadcast/Publish.s.sol/` |
| T-02-07 | Tampering | `Publish.s.sol`, caso negativo con `TAMPER_PROOF` | mitigate | El tamper es intencional y local al script de test/demo (no un input de usuario); el contrato real revierte con `ProofRejected`, probado en Task 2 sin tocar el contrato |
| T-02-08 | Repudiation | `twin/deployments.json` | mitigate | Copiado literal de `ops/deployments.json` de Sobrecito (fuente única, generada por script) hacia el schema `registries[]` que Phase 4 consume; Task 3 lo verifica contra `chainId` y formato de address esperado |
| T-02-09 | Information Disclosure | `.env.example` con addresses | accept | Las addresses de Registry/verifier son públicas por diseño (se leen del explorer); no son secretos, publicarlas en `.env.example` es el propósito de D-08c |

</threat_model>

<verification>
- `Publish.s.sol` compila y reusa `REGISTRY_ADDRESS`/`TAMPER_PROOF`/`CORTE_ID` por env, sin valores de deploy hardcodeados.
- Publish real: receipt con status success on-chain en Arbitrum One, visible en Arbiscan.
- Publish negativo: simulación revierte con `ProofRejected`, sin gastar gas de una tx fallida real.
- `twin/deployments.json` (schema `{registries: [...]}`), `lib/config/tokens.ts` (`REGISTRIES`) y `.env.example` existen y coinciden exactamente con el deploy y publish reales, en el formato que `04-01-PLAN.md`/`04-03-PLAN.md` ya esperan.
- Cero apariciones de la clave privada en cualquier archivo, log generado, o argv de un comando de verificación en este plan.
</verification>

<success_criteria>
SOL-02 cumplido: un publish con la proof de la fixture es aceptado on-chain y visible en el explorer; un publish inválido revierte, demostrado contra el Registry real. El repo twin tiene el registro completo de addresses, en el schema correcto, para que Phase 4 lea el Registry desde la app sin fricción. Fase 2 completa.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sobrecito-registry/02-02-SUMMARY.md`
</output>

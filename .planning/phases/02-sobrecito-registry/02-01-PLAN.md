---
phase: 02-sobrecito-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/broadcast/Deploy.s.sol/42161/run-latest.json
  - /Users/francoperez/repos/job/Sobre/sobrecito/ops/deployments.json
autonomous: false
requirements: [SOL-01]

must_haves:
  truths:
    - "SobrecitoRegistry y HonkVerifier están desplegados en Arbitrum One con el key_hash de circuits/fixtures/manifest.json"
    - "El deploy se autoverifica: keyHash, verifier y publisher on-chain coinciden con lo esperado"
    - "La clave privada del deployer (~/.wakeup-m2-arb1.key) nunca aparece impresa, loggeada ni commiteada"
  artifacts:
    - path: "/Users/francoperez/repos/job/Sobre/sobrecito/contracts/broadcast/Deploy.s.sol/42161/run-latest.json"
      provides: "Broadcast real del deploy en Arbitrum One (chainId 42161), fuente de verdad de addresses y tx hashes"
    - path: "/Users/francoperez/repos/job/Sobre/sobrecito/ops/deployments.json"
      provides: "Registro reproducible del deploy generado por record-deployment.sh (D-08), nunca tipeado a mano"
  key_links:
    - from: "script/Deploy.s.sol"
      to: "circuits/fixtures/manifest.json (.anchors.agg_l1_vk_hash)"
      via: "vm.readFile + vm.parseJsonBytes32 en runtime"
      pattern: "keyHash anclado == manifest == registry.keyHash()"
    - from: "ops/record-deployment.sh"
      to: "contracts/broadcast/Deploy.s.sol/42161/run-latest.json"
      via: "lectura de artefactos de broadcast, jamás addresses tipeadas"
      pattern: "sobrecito_registry.*honk_verifier"
---

<objective>
Deployar SobrecitoRegistry + HonkVerifier en Arbitrum One (mainnet, chainId 42161) desde el repo Sobrecito, con el key_hash anclado a la fixture, tras un preflight completo y el top-up de ETH del usuario en la wallet M2. Este plan NO toca el repo twin: todo el trabajo corre en `/Users/francoperez/repos/job/Sobre/sobrecito`.

Purpose: cumple SOL-01 (Registry + verifier desplegados) y deja el broadcast real + `ops/deployments.json` como fuente de verdad para el publish y el handoff de Plan 02.
Output: `SobrecitoRegistry` y `HonkVerifier` vivos en Arbitrum One, `ops/deployments.json` generado, cero secretos expuestos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/francoperez/repos/twin/.planning/phases/02-sobrecito-registry/02-CONTEXT.md
@/Users/francoperez/repos/twin/SPEC.md

# Canónicos del repo Sobrecito (solo lectura, no se copian)
@/Users/francoperez/repos/job/Sobre/sobrecito/contracts/README.md
@/Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/Deploy.s.sol
@/Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/dry-run-anvil.sh
@/Users/francoperez/repos/job/Sobre/sobrecito/ops/record-deployment.sh
@/Users/francoperez/repos/job/Sobre/sobrecito/circuits/fixtures/manifest.json

<interfaces>
Wallet deployer/publisher (D-01): `0x13B56eA93CB18ae90d7Ff6E01Cb97C1AbFB2B992`, clave en `~/.wakeup-m2-arb1.key`.
RPC (D-06): `https://arbitrum-one-rpc.publicnode.com`, chainId 42161. Configurable por env si aparece una key dedicada.

`Deploy.s.sol` no recibe argumentos: lee el key_hash del manifest, usa `publisher = deployer` por default (no pasar `PUBLISHER_ADDRESS`), y se autoverifica al final del `run()` con 4 `require`. Si algún require falla, el script revierte y NO queda broadcast válido.

`record-deployment.sh --chain-id 42161 --network arbitrum-one --rpc-url <rpc> --explorer-base https://arbiscan.io [--out <path>]` lee `contracts/broadcast/Deploy.s.sol/42161/run-latest.json` + el manifest + `.gas-snapshot`, y escribe (por default) `ops/deployments.json`.

Nunca pasar `--verify` a `forge script` salvo que `ETHERSCAN_API_KEY` exista en el entorno (D-07, best-effort, no bloqueante).

Higiene de clave (aplica a Task 3 de este plan y a Plan 02): el grep de verificación de que la clave no quedó grabada en ningún artefacto NUNCA debe interpolar el contenido de la clave como argumento de shell (queda visible en `ps`/history). Usar `grep -f` con un pattern-file vía process substitution, no `grep -e "$(cat ...)"`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirmar top-up de ETH en la wallet M2 (Arbitrum)</name>
  <what-built>Nada todavía: este es un bloqueo externo real, no automatizable. El balance actual de `0x13B56eA93CB18ae90d7Ff6E01Cb97C1AbFB2B992` en Arbitrum es 0.00053 ETH; el deploy + primer publish necesitan sumar ~0.002 ETH (D-02).</what-built>
  <how-to-verify>
    1. El usuario transfiere ~0.002 ETH (o más, con margen) a `0x13B56eA93CB18ae90d7Ff6E01Cb97C1AbFB2B992` en Arbitrum One (chainId 42161) desde cualquier wallet propia o exchange.
    2. Confirmar: `cast balance 0x13B56eA93CB18ae90d7Ff6E01Cb97C1AbFB2B992 --rpc-url https://arbitrum-one-rpc.publicnode.com --ether`
    3. El balance reportado debe ser >= 0.002 ETH antes de seguir a Task 2.
  </how-to-verify>
  <resume-signal>Responder "topped up" (o el balance confirmado) para continuar. El preflight de Task 2 vuelve a chequear el balance igual, así que un top-up insuficiente aborta ahí con mensaje claro.</resume-signal>
  <read_first>02-CONTEXT.md D-02, D-04</read_first>
  <acceptance_criteria>Balance de la wallet M2 en Arbitrum >= 0.002 ETH confirmado por `cast balance` antes de avanzar.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Preflight obligatorio (D-04)</name>
  <files>Ninguno modificado; solo lectura y comandos de verificación contra el repo Sobrecito.</files>
  <read_first>
    - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/README.md (sección "Cómo se regenera y cómo se despliega")
    - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/dry-run-anvil.sh
  </read_first>
  <action>
    Correr, en orden, desde `/Users/francoperez/repos/job/Sobre/sobrecito/contracts` (D-04):
    1. `cast balance 0x13B56eA93CB18ae90d7Ff6E01Cb97C1AbFB2B992 --rpc-url https://arbitrum-one-rpc.publicnode.com --ether` — abortar con mensaje explícito si da menos de 0.002 ETH (no avanzar a Task 3).
    2. `forge test` — debe terminar en verde (22 tests, incluye los 5 negativos de D-07).
    3. `bash script/dry-run-anvil.sh` — ensayo end-to-end del deploy contra anvil local, sin fondos ni red externa; el script ya trae sus propios asserts sobre el `deployments.json` de prueba y sobre el código on-chain del Registry en anvil.
    No usar `--verify` en ningún comando de este plan salvo que `ETHERSCAN_API_KEY` exista en el entorno (D-07).
  </action>
  <verify>
    <automated>cd /Users/francoperez/repos/job/Sobre/sobrecito/contracts && forge test 2>&1 | tail -5</automated>
  </verify>
  <done>Los tres chequeos (balance >= 0.002 ETH, forge test verde, dry-run-anvil.sh exitoso) pasan sin intervención manual adicional. Si alguno falla, el plan se detiene ahí y no se ejecuta Task 3.</done>
  <acceptance_criteria>Balance suficiente confirmado, `forge test` sale 0 failing, `dry-run-anvil.sh` termina con "dry-run completo" y exit code 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Deploy real en Arbitrum One + record-deployment</name>
  <files>
    - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/broadcast/Deploy.s.sol/42161/run-latest.json (generado por forge)
    - /Users/francoperez/repos/job/Sobre/sobrecito/ops/deployments.json (generado por record-deployment.sh)
  </files>
  <read_first>
    - /Users/francoperez/repos/job/Sobre/sobrecito/contracts/script/Deploy.s.sol
    - /Users/francoperez/repos/job/Sobre/sobrecito/ops/record-deployment.sh
  </read_first>
  <action>
    Desde `/Users/francoperez/repos/job/Sobre/sobrecito/contracts`, con la clave cargada inline por command substitution (D-03, jamás en una variable de entorno exportada ni impresa):

    1. `forge script script/Deploy.s.sol:Deploy --rpc-url https://arbitrum-one-rpc.publicnode.com --private-key "$(cat ~/.wakeup-m2-arb1.key)" --broadcast`

    Regla dura: NUNCA hacer `echo`, `cat`, `set -x` ni ningún comando que exponga el contenido de `~/.wakeup-m2-arb1.key` en stdout, logs o archivos. La clave solo puede aparecer dentro de la command substitution del flag `--private-key`, en un único comando, nunca asignada a una variable de shell que sobreviva ese comando.

    2. Confirmar que `forge script` terminó sin error y que el output de consola muestra `chainId 42161` (no 31337) y las 4 líneas de autoverificación (keyHash, verifier, registry, publisher) sin ningún string de 64 hex chars que luzca como clave privada (la clave nunca se imprime; esto es un chequeo de higiene, no se espera encontrar nada).

    3. Correr `ops/record-deployment.sh` desde la raíz del repo Sobrecito:
       `bash ops/record-deployment.sh --chain-id 42161 --network arbitrum-one --rpc-url https://arbitrum-one-rpc.publicnode.com --explorer-base https://arbiscan.io`

    4. Verificar el `ops/deployments.json` resultante contra la chain real:
       - `cast call <sobrecito_registry> "keyHash()(bytes32)" --rpc-url https://arbitrum-one-rpc.publicnode.com` == `.anchors.agg_l1_vk_hash` del manifest
       - `cast call <sobrecito_registry> "publisher()(address)" --rpc-url https://arbitrum-one-rpc.publicnode.com` == `0x13B56eA93CB18ae90d7Ff6E01Cb97C1AbFB2B992`
       - `cast code <sobrecito_registry> --rpc-url https://arbitrum-one-rpc.publicnode.com` no vacío (código desplegado)

    5. Grep de higiene, SIN interpolar la clave como argumento de shell (visible en `ps`/history si se hace mal): usar `grep -f` con un pattern-file por process substitution, nunca `grep -e "$(cat ...)"`.
       `grep -arF -f <(sed 's/^0x//' ~/.wakeup-m2-arb1.key) /Users/francoperez/repos/job/Sobre/sobrecito/contracts/broadcast/ /Users/francoperez/repos/job/Sobre/sobrecito/ops/deployments.json`
       Debe devolver vacío (0 matches, exit code 1 de grep) — evidencia de que la clave no quedó grabada en ningún artefacto. Si por algún motivo devuelve matches, tratarlo como fallo bloqueante del plan.
  </action>
  <verify>
    <automated>cast call $(python3 -c "import json;print(json.load(open('/Users/francoperez/repos/job/Sobre/sobrecito/ops/deployments.json'))['sobrecito_registry'])") "keyHash()(bytes32)" --rpc-url https://arbitrum-one-rpc.publicnode.com</automated>
  </verify>
  <done>`ops/deployments.json` existe con `chainId: 42161`, `sobrecito_registry` y `honk_verifier` con código on-chain real, `keyHash` on-chain == manifest, `publisher` == wallet M2, y el grep de higiene de la clave (vía pattern-file, sin exponerla en argv) da 0 matches.</done>
  <acceptance_criteria>SOL-01 satisfecho: Registry y verifier desplegados en Arbitrum One vía Deploy.s.sol con el key_hash de la fixture, autoverificados y registrados en `ops/deployments.json` sin exponer la clave privada, ni siquiera en argv del propio chequeo de higiene.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Filesystem clave -> proceso Foundry | `~/.wakeup-m2-arb1.key` cruza de un archivo local a un flag de proceso, vía command substitution |
| Operador -> RPC público | `forge script`/`cast` hablan con `arbitrum-one-rpc.publicnode.com`, un endpoint público de terceros |
| Deployer EOA -> contrato on-chain | El deploy fija `owner` y `publisher` como el mismo EOA (D-01), sin multisig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-02-01 | Information Disclosure | Task 3, carga de `~/.wakeup-m2-arb1.key` | mitigate | Command substitution inline en un único comando (D-03); prohibido `echo`/`set -x`/variables de entorno persistentes; grep de higiene post-deploy vía pattern-file (`grep -f <(...)`, nunca la clave en argv) sobre broadcast y deployments.json |
| T-02-02 | Tampering | `SobrecitoRegistry.publish` (fuera de este plan, ya deployado) | accept | El gateo por `verify` UltraHonk y los checks `KeyHashMismatch`/`CLMismatch` ya están probados por los 22 tests de Foundry (D-04 preflight los re-corre); no se toca el contrato en este plan |
| T-02-03 | Elevation of Privilege | Deploy.s.sol, asignación de `publisher` | mitigate | Deploy.s.sol assertea `registry.publisher() == publisher` (deployer por default, D-01); Task 3 vuelve a verificar con `cast call` contra la chain real |
| T-02-04 | Repudiation | `ops/deployments.json` | mitigate | Generado exclusivamente por `record-deployment.sh` desde artefactos de broadcast medidos (D-08); jamás addresses tipeadas a mano |
| T-02-05 | Denial of Service | RPC público `publicnode.com` | accept | Riesgo de demo de una sola corrida; D-06 deja el RPC configurable por env si aparece uno dedicado; no bloquea el criterio de éxito de la fase |

</threat_model>

<verification>
- `forge test` verde (22/22) antes de cualquier broadcast.
- `dry-run-anvil.sh` exitoso contra anvil local.
- Balance de la wallet M2 >= 0.002 ETH confirmado antes del broadcast real.
- `ops/deployments.json` generado con addresses, tx hashes y key_hash que coinciden con la chain real (Arbitrum One, 42161).
- Cero apariciones de la clave privada en cualquier archivo generado, log de consola, o argv de un comando de verificación.
</verification>

<success_criteria>
SobrecitoRegistry y HonkVerifier desplegados y autoverificados en Arbitrum One (chainId 42161) vía Deploy.s.sol, con el key_hash de la fixture anclado y `ops/deployments.json` registrado. SOL-01 cumplido. Plan 02 puede arrancar con las addresses reales.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sobrecito-registry/02-01-SUMMARY.md`
</output>
